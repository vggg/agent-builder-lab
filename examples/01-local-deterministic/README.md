# Example 01: local deterministic plan

From the repository root:

```bash
PYTHONPATH=src python -m agent_builder_lab --scenario scenarios/research.yaml --json trace.json
```

This example proves only that the pipeline and decision trace are executable and reproducible. Catalog entries are fixtures whose capabilities are claimed, not independently verified. No agent is invoked in this milestone.

